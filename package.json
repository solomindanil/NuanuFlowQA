{
  "name": "nuanuflowqa",
  "version": "1.0.0",
  "description": "Agent-driven QA platform: automated product testing with bug filing to Linear and fix-prompt generation",
  "type": "module",
  "private": true,
  "scripts": {
    "build:paydemo": "PAYDEMO_VARIANT=fixed-v2 node scripts/build-paydemo.mjs",
    "start:paydemo": "npm run build:paydemo && PAYDEMO_VARIANT=fixed-v2 node apps/paydemo/server.mjs",
    "test:paydemo:harness": "node --test tests/paydemo-probe/*.mjs tests/paydemo-synthesize/*.mjs tests/paydemo-environment/*.mjs",
    "test:paydemo:provenance": "node --test tests/paydemo/build-provenance.test.mjs",
    "test:paydemo:identity": "node --test tests/paydemo/server-identity.test.mjs",
    "test:paydemo": "PAYDEMO_SUITE=fixed PAYDEMO_VARIANT=fixed-v2 playwright test --config=playwright.paydemo.config.ts",
    "test:paydemo:bugs": "PAYDEMO_SUITE=controlled-bug PAYDEMO_VARIANT=buggy-v1 playwright test --config=playwright.paydemo.config.ts",
    "verify:paydemo": "npm run build:paydemo && npm run test:paydemo:harness && npm run test:paydemo:provenance && npm run test:paydemo:identity && npm run test:paydemo && npm run test:paydemo:bugs",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3"
  }
}
